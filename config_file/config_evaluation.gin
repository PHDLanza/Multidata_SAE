include '../config_file/config_general.gin'


eval_visual_hypotheses:
    path_embeddings=%PATH_EMBEDDING_LLAVA
    path_dataset=%PATH_DATASET_LLAVA

    path_dictionary_neurons='path-to-folder' # Path to save the output
    device=%DEVICE

eval_textual_hypotheses:
    path_embeddings=%PATH_EMBEDDING_LLAVA
    path_dataset=%PATH_DATASET_LLAVA

    path_dictionary_neurons='path-to-folder' # Path to save the output
    device=%DEVICE

