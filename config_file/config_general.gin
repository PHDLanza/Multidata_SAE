DEVICE='cuda:0' # Device to use (e.g., 'cuda:0' for GPU)

SAE_PATH='my-models/sae-checkpoint'  # Path to the SAE checkpoint
LABEL_TRAIN_COCO_PATH='path-to-dataset' # Path to the VQA-X train labels

LABEL_TEST_COCO_PATH='path-to-dataset' # Path to the VQA-X test labels

DATASET_COCO_PATH='path-to-dataset' # Path to the COCO dataset

EMBEDDING_COCO_PATH='path-to-folder' # Path to the COCO feature embeddings

EMBEDDING_LLAVA_PATH='path-to-folder' # Path to the LLaVA feature embeddings and intermediate results
DATASET_LLAVA_PATH='path-to-dataset' # Path to the LLaVA dataset

